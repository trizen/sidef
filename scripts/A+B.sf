#!/usr/bin/ruby

while (
        "Insert two integers: ".print;
        var ab = (STDIN.readline.split)
              -> len == 2
      ) {
    "%s + %s = %s\n".printf(ab..., ab.map{_.toNum}->sum);
}

__END__

#
## Simpler
#

Sys.println(STDIN.readline.split.map{_.toNum}->sum);
