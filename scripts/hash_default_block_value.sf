#!/usr/bin/ruby

#
## Block default value for hashes
#

var hash = Hash.new;

var x = 0;
hash.default { x += 1 };

say hash[:a];    # prints: 1
say hash[:b];    # prints: 2
say hash[:c];    # prints: 3

assert_eq(hash[:a], 1);
assert_eq(hash[:b], 2);
assert_eq(hash[:c], 3);
