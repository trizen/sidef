#!/usr/bin/ruby

var a = [1,2,3]
var(x, y) = a...
assert_eq(x, 1)
assert_eq(y, 2)

var(i, j) = a...    # some comment after
assert_eq(i, 1)
assert_eq(j, 2)

assert_eq(1...3, 1.to(3))
assert_eq(1 ... 3, 1.to(3))

var r = (1 ...
         3)

assert_eq(r, 1.to(3))

say "** Test passed!"
