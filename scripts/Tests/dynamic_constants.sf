#!/usr/bin/ruby

#
## In Sidef<=2.30, this test failed under perl-5.18.*
#

func f(a) {
    const x = a;
    x + 2;
}

assert_eq(f(40), 42)
assert_eq(f(33), 42)        # previous value is preserved

say "** Test passed!"
