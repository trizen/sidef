#!/usr/bin/perl

%w(a b).map{Sys.scanln("** Integer " + _ + ": ").to_i}
       .assignTo(\var a, \var b);

for (%w(+ - * / % ** ^ | & << >>)) { |op|
    Sys.printf("%s %s %s = %s\n", a, op, b, a->$op(b));
}
