#!/usr/bin/ruby

while (
        "Insert two integers: ".print;
        var ab = (STDIN.readline.split)
              -> len == 2
      ) {
    "%s + %s = %s\n".printf(ab..., ab.map{.to_num}.sum);
}

__END__

#
## Simpler
#

Sys.println(STDIN.readline.split.map{.to_num}.sum);
