#!/usr/bin/ruby

class Math::Algebra::Matrix;

var VERSION = 0.1;

#
## Require a Perl module
#

var req = (require 'Math::Matrix');

if (nil == req) {
    "** Math::Matrix module is not intalled!\n".warn;
    return();
}

#
## Function declaration
#

func det (matrix) {

    var math = (
        req.new(matrix->to_list);
    );

    Num.new(math.determinant);
}
