#!/usr/bin/ruby

class Math::Algebra::Matrix (matrix) {

    var VERSION = 0.1;

    #
    ## Require a Perl module
    #

    var req = (
            try   { require 'Math::Matrix' }
            catch { Sys.warn("** Math::Matrix is not installed!\n"); return }
    );

    #
    ## Function declaration
    #

    func det (self) {

        var math_matrix = (
            req.new(self.matrix->to_list);
        );

        Num.new(math_matrix.determinant);
    }

}
