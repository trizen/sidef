#!/usr/bin/ruby

# Hash1
var hash1 = Hash.new(
    :larry => 'wall',
    :rob   => 'pike',
);
hash1.default("Unknown");

# Hash2
var hash2 = Hash.new(
    :a => 1,
    :b => 2,
);
hash2.default(0);

#
## Tests
#
hash1[:trizen] == "Unknown" || "hash1 error".die;
hash2[:c] == 0              || "hash2 error".die;


hash1[:larry] == "wall" || "hash1 fetch error".die;
hash2[:b]     == 2      || "hash2 fetch error".die;

# All OK!
"** Test passed".say;
