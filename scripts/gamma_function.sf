#!/usr/bin/ruby

# http://en.wikipedia.org/wiki/Gamma_function

define e  (Math.e);
define pi (Math.pi);

func Γ(t) {
    t < 20 ? (__FUNC__(t + 1) / t)
           : (Math.sqrt(2*pi*t) * Math.pow(t/e + 1/(12*e*t), t) / t);
}

{ |i|
    say "#{i-1}! == #{Γ(i).ceil.int}";
} * 10;
