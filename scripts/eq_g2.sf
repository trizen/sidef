#!/usr/bin/ruby

func g2(a,b,c) {
    var dt = (b**2 - 4*a*c);
    var x1 = ((-b + dt.sqrt) / 2*a);
    var x2 = ((-b - dt.sqrt) / 2*a);
    return [x1, x2];
}

# x^2 + 2x - 8 = 0
g2(1, 2, -8).dump.say;
