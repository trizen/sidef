#!/usr/bin/ruby

var file = (File.new(SCRIPT_NAME));

if (file.open_r(\var fh)) {
    "File successfully accessed!".say;

    fh.readline == "#!/usr/bin/ruby\n"
        || ("fh.readline error!\n".die);
}
else {
    "Can't open the file for reading!\n".die;
}
