#!/usr/bin/ruby

#
## Code interpolation:
#
#         "Hello #{name}"
## means: "Hello" + ({name}.run.to_s)
#

5.times { |i|
    print "#{i} + #{i} == #{i + i}\t";
    print "2*i == 2*#{i} => #{ 2*i }"+"\n";
}
