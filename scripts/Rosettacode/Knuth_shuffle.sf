#!/usr/bin/ruby

#
## http://rosettacode.org/wiki/Knuth_shuffle
#

func shuffle (a) {
 
    { |n|
        var k = (n + 1 -> rand.int);
        k == n || (a[k, n] = a[n, k]);
    } * a.offset;
 
    return a;
}

var arr = 1..10;
say shuffle(arr);
