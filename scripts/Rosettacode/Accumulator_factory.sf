#!/usr/bin/ruby

#
## http://rosettacode.org/wiki/Accumulator_factory
#

class Accumulator (sum) {
    method add(num) {
        self.sum += num;
    }
}
 
var x = Accumulator(1);
x.add(5);
Accumulator(3);
say x.add(2.3);               # prints: 8.3
