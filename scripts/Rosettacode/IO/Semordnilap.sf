#!/usr/bin/ruby

#
## http://rosettacode.org/wiki/Semordnilap
#

var c = 0;
var seen = Hash.new;
seen.default(0);

ARGF.each { |line|
    line.chomp!;
    var r = line.reverse;
    (seen[r]++ && (c++ < 5) && say "#{line} #{r}") || seen[line]++;
}

say c;
