#!/usr/bin/ruby

# Translation of: http://rosettacode.org/wiki/Jensen%27s_Device#Perl

func sum (i, lo, hi, term) {

    var temp = 0;
    for (*i = lo; *i <= hi; (*i)++) {
        temp += term.call;
    }

    return temp;
}

say sum(\var i, 1, 100, { 1 / i });
