#!/usr/bin/ruby

var files = %w(
                /tmp/a.txt
                b.txt
                c.txt
              );

files.map{.toFile}.each { |file|
    say file.edit { |line|
        line.gsub("Goodbye London!", "Hello New York!");
    };
}
